import type TransformingNetworkClient from '../../../communication/TransformingNetworkClient';
import { type IssuerData } from '../../../data/issuer/IssuerModel';
import type IssuerModel from '../../../data/issuer/IssuerModel';
import type Callback from '../../../types/Callback';
import Binder from '../../Binder';
import { type Parameters } from './parameters';
export default class ProfileGiftcardIssuersBinder extends Binder<IssuerData, IssuerModel> {
    protected readonly networkClient: TransformingNetworkClient;
    constructor(networkClient: TransformingNetworkClient);
    /**
     * Enable a gift card issuer on a specific or authenticated profile to use it with payments.
     *
     * @since 3.7.0
     * @see https://docs.mollie.com/reference/enable-method-issuer
     */
    enable(parameters: Parameters): Promise<IssuerModel>;
    enable(parameters: Parameters, callback: Callback<IssuerModel>): void;
    /**
     * Disable a gift card issuer on a specific or authenticated profile.
     *
     * @since 3.7.0
     * @see https://docs.mollie.com/reference/disable-method-issuer
     */
    disable(parameters: Parameters): Promise<true>;
    disable(parameters: Parameters, callback: Callback<true>): void;
}
